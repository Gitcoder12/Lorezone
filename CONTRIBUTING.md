# Contributing to Lorezone
- Backend: Node/TypeScript in `backend/src/`
- Frontend: Next.js in `frontend/src/`
- DB schema: `backend/schema.sql`
- Run locally: `docker-compose up` or see README for manual setup
- New media types: add to `frontend/src/app/types.ts` and update browse pages
